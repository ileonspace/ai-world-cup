from __future__ import annotations

MANUAL_WORKFLOW = [
    "Generate prompt from the repository.",
    "Copy the full prompt.",
    "Send the exact same prompt to each LLM.",
    "Save the raw answer exactly as returned.",
    "Import the answer into the repository.",
]
